{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach Node in Docker (9229)",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 9229,
            "restart": true,
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/home/app" // debe coincidir con WORKDIR de tu contenedor
        }
    ]
}